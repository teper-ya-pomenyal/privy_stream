POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB_USERS=users_db
USER_CACHE_PASSWORD=
STREAMING_SERVICE_PORT=50054
CATALOG_WRITE_SERVICE_PORT=50056

# Домены веб-клиента через запятую (схема обязательна), например https://app.example.com.
# Десктопный клиент (Tauri) разрешён всегда.
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:1420
